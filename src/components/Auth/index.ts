import _routes from './router';
import * as _repository from './repository';
import _consumers from './consumers';

export const routes = _routes;
export const repository = _repository;
export const consumers = _consumers;